// Package ledger defines the finance ledger interfaces (chart of accounts,
// journal entries, append-only journal lines). It is intentionally empty
// at Phase A; Phase C adds the posting engine and balance invariants.
package ledger
